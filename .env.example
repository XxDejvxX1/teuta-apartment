# Copy to .env.local and fill in.

# Public origin, used for canonical URLs, hreflang alternates, sitemap and the
# social preview image. Must be the real domain in production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
